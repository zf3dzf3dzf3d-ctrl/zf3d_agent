# 朱峰社区智能体

**朱峰社区智能体**（ZF3D Agent）是一个开源的全JSON配置驱动的桌面AI智能体框架。纯Python标准库实现，零外部依赖，复制即运行。支持10种大语言模型，内置131种操作，涵盖文件管理、代码开发、AI生图/视频、PS级图片加工、浏览器自动化、Office文档等领域。Ctrl+~全局呼出轮盘快捷菜单，随时随地翻译、问答、截图、朗读。

## 概述

"系统是手脚，大模型是大脑"——系统负责调度、搬运、执行，大模型负责思考、决策、优化。用户通过JSON配置定义一切行为，代码只是引擎，配置才是灵魂。不绑定任何SDK，纯HTTP直连，可适配任意OpenAI兼容API。公共区（可发布）与隐私区（绝不外泄）严格分区，密钥XOR+机器码加密。

## 核心亮点

- 🔥 **零依赖复制即用** — 纯Python标准库，不装pip包，不配环境，复制文件夹双击即跑
- 🧠 **ReAct推理引擎** — 思考→行动→观察→校验循环，Function Calling+文本双模式智能降级，任务经验自动提炼与召回
- 🎛️ **Ctrl+~全局轮盘** — 任何程序中选中文本，按Ctrl+~呼出轮盘：翻译/问答/朗读/截图，设置面板全可视化自定义
- 💬 **问答连通主对话** — 快速浮窗问答自动注入最近10轮主对话历史，结果回写SQLite，网页端无缝衔接
- 🎨 **PS级图片加工** — 图层混合、11种笔刷工具、选区布尔运算、滚轮缩放，专业级编辑器内置
- 🤖 **131种内置操作** — 文件/代码/Git/网络/Office/ComfyUI/浏览器/图片处理，5层工具体系可无限扩展
- 🧬 **双引擎自进化** — 测试员找Bug→开发者改代码→审查员审查，AI自己优化自己，人工把关合并
- 🔒 **14重安全防线** — 文件权限弹窗、禁止路径硬编码、AST安全求值、XSS防护、审计日志、密钥加密
- 🇨🇳 **全中文命名** — 函数、变量、文件夹、配置全中文，中国开发者零门槛上手

## 版本历史

| 版本 | 日期 | 重点更新 |
|------|------|----------|
| v2.0 | 06-25 | 模块化重构，ReAct引擎，事件制记忆，双引擎自进化，40操作 |
| v2.1 | 06-26 | 静默自动更新(多源回退)，EXE打包，Gitee镜像 |
| v2.2 | 06-29 | 文件拖拽移动/复制，画廊分页，去背景工具，股票LSTM预测 |
| v2.3 | 06-30 | 图片加工专业版(图层+11工具+选区)，经验师模块，安全修复 |
| v2.4 | 06-30 | 音乐/视频播放系统，多线程下载(aria2c加速+SHA256校验) |
| v2.7 | 07-02 | Bug追踪器，存储引擎读写分离，性能提升10-15倍 |
| v2.8 | 07-02 | 修复4个核心Bug(工具参数/异常兜底/搜索别名/Diff查看器) |
| v2.9 | 07-03 | 快速浮窗翻译弹窗，问答连通主对话，轮盘设置面板(热更新) |

## 技术架构

### 内核（19个组件）
启动器、配置加载器、模型直连器、网页服务、文件管理器、操作注册中心、安全计算器、存储引擎(SQLite+FTS5)、运行诊断器、Bug追踪器、技能/插件/MCP加载器、快速浮窗、全局呼出器、截图选区、定时任务等。

### ReAct推理引擎
状态机驱动思考→行动→观察→校验→终止循环，支持Function Calling与文本双模式。按需上下文压缩防止撞token上限。任务成功后经验师自动提炼可复用经验，任务前召回匹配经验注入提示词，AI越用越聪明。

### 子代理系统
| 类型 | 职责 | 权限 |
|------|------|------|
| explore | 只读搜索 | 搜索/读取/列目录 |
| general | 执行任务 | 所有操作 |
| plan | 规划师 | 生成计划不执行 |
| reviewer | 质检员 | 只读+验证代码 |

### 记忆系统
事件制存储，每段对话归类为一个事件，三层语义召回（标签→关键词→LLM语义）。自动生成摘要和用户画像，长任务支持检查点续跑。

### 五层工具体系
1. **内置操作**（131个）
2. **声明式动态工具**（JSON声明即加载，http/command/python三种）
3. **插件**（放入.py文件即启用，importlib动态加载）
4. **技能包**（SKILL.md指令注入+脚本包装为操作）
5. **MCP服务**（JSON-RPC stdio自动发现外部工具）

## 快速浮窗（轮盘）

按 **Ctrl+~** 任何程序中呼出，以鼠标为中心展开轮盘：

| 扇区 | 功能 |
|------|------|
| 🔤 翻译 | 选中文本翻译，弹窗显示原文/译文/介绍/造句，流式输出+TTS+拖拽 |
| 💬 问答 | 多轮对话，自动注入主对话历史，结果回写SQLite，自动TTS播报 |
| 📢 朗读 | 选中文本TTS语音朗读 |
| 📷 截图 | 框选区域截图，绘图标注/OCR识别/翻译/保存/复制 |

设置→🎛️轮盘：快捷键、半径、字体、透明度、7项颜色、扇区增删排序，保存后热更新无需重启。

## 主要功能

| 功能 | 说明 |
|------|------|
| 💬 对话 | ReAct推理+流式输出+语音播报，131种操作随时调用 |
| 📁 文件管理 | 拖拽移动/复制，悬浮按钮，框选批量，分页加载，我的电脑 |
| 📝 编辑器 | 自研语法高亮+代码提示，文件树重命名 |
| 🎨 图片加工 | PS风格专业编辑器：图层+11种工具+选区+混合模式 |
| 🧹 去背景 | 上传图片自动去背景，生成透明PNG |
| 🖼 画廊 | 图片/视频/音频浏览播放 |
| 🎵 音乐播放 | 在线搜索→下载→ffmpeg转MP3→播放栏 |
| 🎬 视频播放 | 在线搜索→下载流→合并MP4→本地播放 |
| 🤖 AI生图/视频 | ComfyUI集成：文生图/图生图/文生视频/反推/工作流(14操作) |
| 🌐 浏览器自动化 | Playwright：打开/截图/点击/表单/会话(14操作) |
| 📄 Office | Word读取/修改/创建/段落，Excel替换，保留格式 |
| 🧠 记忆 | 事件记忆+用户画像+经验召回 |
| 📊 Token统计 | 按模型聚合统计，调用历史 |
| 🧬 自进化引擎 | 三智能体：测试员→开发者→审查员 |
| 🎛️ 轮盘 | Ctrl+~全局快捷菜单，全可视化设置 |

## 支持的模型（10种）

| 模型 | 开发商 | Vision |
|------|--------|--------|
| DeepSeek | 深度求索 | — |
| 通义千问 | 阿里云 | ✅ |
| 智谱GLM | 智谱AI | ✅ |
| Kimi | 月之暗面 | ✅ |
| 豆包 | 字节跳动 | ✅ |
| ChatGPT | OpenAI | ✅ |
| Claude | Anthropic | ✅ |
| Gemini | Google | ✅ |
| 本地Qwen3 | Ollama | — |
| 自定义 | 任意兼容端点 | — |

## 安全机制

文件权限弹窗+3次放行、禁止路径硬编码、AST安全求值、XSS防护、路径穿越防护、密钥加密存储、审计日志、主引擎保护、隐私分区、发布打包泄露扫描等14重防线。

## 双引擎研发流水线

主引擎（公共区，稳定版）与工作引擎（隐私区，实验版）分离。三智能体自进化：测试员找Bug→开发者改代码→审查员审查，四重检测（语法+导入+配置+回归），人工决定合并，保留5个备份可回滚。

## 技术规格

| 指标 | 数值 |
|------|------|
| 语言 | Python 3.8+（纯标准库，零依赖） |
| Python文件 | 70个 |
| 代码行数 | 31,900+行 |
| 内置操作 | 131个 |
| 测试用例 | 246个 |
| 支持模型 | 10种 |
| 配置文件 | 13个JSON |
| 前端 | 纯HTML/CSS/JS（无框架） |
| 存储 | JSON + SQLite(FTS5) |
| 许可证 | MIT License |

## 设计哲学

> 够用就行，越小越好。
> 系统是手脚，大模型是大脑。
> 一切都是数字资产，尊重每一行代码。
> 公共区是脸面可发布，隐私区是里子绝不外泄。
> 遇到问题不自己想，打包丢给大模型。
> 用户通过JSON定义一切行为，代码只是引擎，配置才是灵魂。
> 所有路径相对引用，复制到哪里都能跑。

## 外部链接

- **GitHub**：https://github.com/zf3dzf3dzf3d-ctrl/zf3d_agent
- **Gitee镜像**：https://gitee.com/zf3d/zf3d_agent（国内推荐）
- **本地运行**：http://localhost:8765
- **QQ群**：290939358
- **许可协议**：MIT License
