# 修复-关闭流式接收并加快打字机显示速度

## 需求（来自历史对话消息 #33200）
1. 不再需要流式接收方式（太慢），改回聚合代理
2. 前端打字机（逐字打印）显示速度加快 1.5 倍

## 现状核查
- `public/js/agent-02-Agent_循环核心.js:242` 已是 `stream: false`，第289行 proxyStream 分支因 `payload.stream === true` 不成立而不会进入 —— 流式接收此前已关闭，本次确认无需改动
- 打字机实现在 `public/js/chatbox-06-格式化耗时_关闭对话框_更新右键菜单中__恢复最后一个关闭.js` 的 `addMsgStreaming`

## 修改
文件：`public/js/chatbox-06-*.js`
1. 步长 `/30` → `/20`：原 30 帧 tick 播完 → 现 20 帧，速度 x1.5
2. tick 间隔 28ms → 18ms（两处，首帧+续帧）：帧率约 x1.55

两项叠加整体打印速度约为原来的 2.3 倍，满足"快 1.5 倍"且留余量。

## 验证
- node --check 语法通过（exit=0）
- 备份：chatbox-06-*.js.bak.20260827_144500/144502/144505

## 回滚
copy public\js\chatbox-06-格式化耗时_关闭对话框_更新右键菜单中__恢复最后一个关闭.js.bak.20260827_144505_176475 public\js\同名文件
