# 模型配置变更记录

- [2026-08-27] 火山方舟模型统一为 glm-5.3 补漏完成。主版本 public/config/models.json 中「火山方舟」「火山Coding Plan」两条通道的 displayName 和 version 残留 deepseek-v4-flash 已改为 glm-5.3（modelId 之前已是）。工作版本5 三处配置此前已全部到位，本次未改动。
- 注意事项：
  1. models.json 的 modelIdOptions 数组是可选模型ID列表（含 deepseek-v4-flash 等），属于正常可选项应保留，不要当作残留清掉。
  2. 「火山方舟视觉 (Seedream 5.0 Lite)」是 imageGen:true 的生图通道，modelId 为 doubao-seedream-5.0-lite 是正确的，不要误改为语言模型 glm-5.3。
  3. 主版本 public/js/models.js 的 defaultList() 已返回空数组，配置真正生效来源是后端 /api/models/config → public/config/models.json + private/api_keys.json（model_config.py 合并拆分）。
  4. 工作版本5 的 public/js/models.js defaultList() 是 localStorage 硬编码版，默认通道已是「火山方舟(glm-5.3)」。