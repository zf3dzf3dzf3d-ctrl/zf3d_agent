# 图片视频查看面板重做（2026-08-27）

## 问题
面板挂在 canvasContent 画布内容层（absolute 定位），保存的坐标是画布坐标系，画布平移/缩放后默认跑到左边 (0,0)。

## 重做方案
- **public/js/app-imageviewer.js**：重写。
  - 面板挂 `document.body`，CSS `position: fixed` 视口定位 —— 彻底脱离画布变换，不再跑位
  - 默认位置 = 视口右上角（defaultPos 计算）；记忆位置非法/飘出视口时自动回右上角；openPanel 再钳制一次
  - 双击面板/图片 → 最大化覆盖视口（留 8px 边距），再次双击/✕/Esc 还原小窗（✕ 在最大化时先退回）
  - 滚轮以鼠标为中心缩放 zoomAt（0.05x–40x）；左键按住拖拽平移媒体
  - 视频模式保留原生控件、自动播放；双击在 video 控件上不触发最大化
  - 删除 canvasScaleFactor 换算逻辑（fixed 坐标不需要）
- **public/css/app-imageviewer.css**：.iv-float-panel 改 fixed；z-index 100010/100020；其余样式保持
- index.html 缓存版本号 css v=3→v=4、js v=6→v=7

## 交互汇总
- 标题栏拖拽移动（记忆位置）｜右下角手柄调尺寸（记忆尺寸）｜工具条 －/%/＋/适应
- 提示文案：「双击最大化 · 滚轮缩放 · 拖拽平移」

## 验证
node --check 通过
