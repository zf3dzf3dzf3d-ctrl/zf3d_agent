# -*- coding: utf-8 -*-
"""路径守卫（已停用）。

原功能：写类工具只允许改发布版目录/项目目录内的文件，越界拒绝写入。
2026-09-11 按要求彻底去掉此限制：永远放行，仅保留函数签名兼容旧调用。
"""


def guard_write_path(path, project_dir=None):
    """永远放行。返回 (True, '')。保留签名以兼容旧调用方。"""
    return True, ''
