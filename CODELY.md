

## Codely Structured Memories

### User

### Feedback
- [2026-09-10 12:32:56] [2026-09-10 12:38] 用户规则：启动器绝不自动安装依赖（原话"初始不要自动安装东西"）。已从 .启动朱峰社区智能体无限.bat 移除「import numpy 检测 → call .安装依赖.bat」整块（该检测必失败：numpy 缺失+pip 本体损坏，安装器永远装不上还假报"全部完成"，每次冷启动白耗 ~25 分钟，playwright chromium 大文件在 USB Wi-Fi 上反复 ERR_SSL_DECRYPTION_FAILED_OR_BAD_RECORD_MAC）。.安装依赖.bat 保留为纯手动工具；设置面板「组件下载」(server/routes/mixin_components.py) 是官方替代安装路径。**Why:** pip 坏→自动安装必失败且拖慢每次启动，用户明确不要。**How to apply:** ①以后改启动脚本不得重新加入任何自动安装/自动下载逻辑；②要修依赖走 get-pip.py 引导 pip（路径见 2026-09-09 17:33 记录），修好之前别让启动器碰 pip；③playwright+chromium-headless-shell 已装好且浏览器自动化在用，仅 numpy 等多媒体依赖缺失（音频/视频/屏幕录制工具受影响，核心对话不受影响）；④启动 bat 改动时注意文件是 GBK 编码（chcp 936），中文注释勿存成 UTF-8。

### Project
- [2026-09-09 17:33:28] [2026-09-09 17:40] 发布版内置python(3.11.9)残缺：pip包本体+ensurepip缺失（仅剩pip-26.2.1.dist-info空壳和Scripts\pip.exe），.安装依赖.bat每次启动必失败且末尾假报"全部完成"；实测缺numpy/playwright（requests/bottle/psutil在）→浏览器自动化/音频录制/屏幕录制/视频工具运行时报ModuleNotFoundError，核心对话/代理/闸门不受影响。用户2026-09-09选择暂不修复；修复路径=get-pip.py引导pip后装numpy+pillow+playwright+chromium。另：启动bat的"[ERROR] Server exited with an error"窗口若上方无Python traceback且server\crash.log不存在=restart_server --force强杀旧进程的预期退出（taskkill退出码1），非崩溃，窗口可直接关闭。**Why:** 避免重复诊断依赖缺失、避免把重启强杀误判为服务器崩溃。**How to apply:** 工具报No module named playwright/numpy先想到此根因；遇[ERROR]退出窗口先查traceback/crash.log有无来区分真崩溃与强杀。
- [2026-09-09 17:43:46] [2026-09-09 17:50] 朱峰智能体与Codely深度对比+提升总结已落盘：docs/提升总结-对比Codely-2026-09-09.md（〇~七章：审读文件清单/领先项12条/提升建议A1-A8·B1-B7·C·D·E·F·G·H系/不建议改红线10条/Codely上下文纪律+验收机制第一人称拆解/路线图29行）。核心未修P0发现：①A8——tools/coding/backend/replace_text.py count==0时返回ok:true+replacements:0（零匹配假成功，16:31"报success改动蒸发"事故根因），paths多文件模式整包ok:true淹没个别error；②B6——_preflight.py与_changelog.py两个模块在发布版不存在：write.py L88/replace_text.py L40"语法预检闸门"、write/replace_text/task_complete变更溯源记账结算全是try/except:pass静默吞掉的死代码，磁盘无项目记录/变更日志.md与文件变更索引.md。**Why:** 机械验收判据不能挂在死代码和假信号上；当日"假成功三连"（A8工具层+B6防线层+安装依赖bat打包层）同病根="相信成功了"≠"真的成功了"。**How to apply:** 后续按该文档落地A/B系建议前先读此文档对齐现状；最高优先=修replace_text零匹配报错(~5行)+补齐两个缺失模块+启动时import断言自检；replace_text的"多处命中拒绝"已与Codely持平勿重复开发。
- [2026-09-09 23:09:11] [2026-09-09 23:10] chat_gate v4.5「对话优先级两档」上线并实战验证（用户规格："第一档他加速其他减速，第二档他加速其他停止给他让道；同一个大模型；不同大模型不参与优先级"）：①档1加速=插队优先放行+同模型普通票放行间隔×3（_PRIO_DECEL_MULT，gap=0 时有 0.3s 下限防失效）；档2让道=插队+同模型普通票完全让停（state 仍 queued、held=True 面板可见），档2跑完另有 20s 独占保持期（_PRIO_GRACE，防 Agent 多轮循环间隙被钻空）；作用域按票面 provider 动态判定，档2可压制档1；空闲10分钟自动降回（_PRIORITY_TTL，每请求刷新），仅内存不落盘（重启回普通）。②实现：_pick_ticket_locked 优先级选票（优先级降序→入队升序）、_prio_providers_locked、set_box_priority、priorities_view；控制 POST gate/control {action:'set_priority',box,tier}；前端对话框头部⚡按钮循环三态（chatbox-00/03/06 → v483/489/488，创建与恢复两套模板都加了按钮）；active_snapshot 增 priorities/priority/held。③测试 17 项全过+接口冒烟过；与 chat_pool v1.0 兼容（同走 acquire(entry='pool')，优先级自动覆盖池路径）。**关键坑（e2e 抓到）**：worker 改优先级选票后 queue 退化为纯唤醒信号——Empty 超时分支【不能 continue】，必须落到底部重扫 _pick_ticket_locked，否则"唤醒名额被优先票顶替"的滞留票在物理队列空了之后永远没人捞（症状=票永久 queued）；退役判定同理必须查 _active 逻辑残留而非只看队列空（旧"队列空=无残留"前提在选票制下不再成立）。
- [2026-09-10 06:36:50] [2026-09-10 06:40] chat_pool v1.0「全请求头丢失→上游401」事故已修复（06:36重启PID 8360生效）：根因=pool_worker._engine_preprocess 中 `headers.clear(); headers.update(eng_ret['headers'])`——zf_core 引擎返回的就是传入 ctx 本身，eng_ret['headers'] 与 headers 同一对象，clear() 把自己清空→Authorization 全丢→池路径所有请求无鉴权头→火山401"missing or invalid"/智谱401"未收到Authorization参数"。旧路径(mixin_proxy L199)用变量重绑定 `headers=_eng_ret['headers']` 无此问题。修复=合并前同一性判断（`_ret_headers is not headers` 才 clear+update，payload 同理），备份 pool_worker.py.bak.20260910_064800_401fix。**Why:** 昨晚池 e2e 全打 127.0.0.1 mock（不校验 Authorization），头丢失零暴露；今早首批真实 ark 流量 100% 401（c63/9jk/lzl 三个turn），key 无辜（ark-...13ec5 直 curl 200 有效）。**How to apply:** ①改池/代理层后必须用真实上游（或校验 Authorization 的 echo server）做一次冒烟；②诊断 401 先判"头到底有没有到上游"：Ark 报"missing or invalid"兼容头缺失，用同一掩码key分别打旧 /api/proxy 与 /api/chat-pool/chat 可秒判哪条路径坏；③引擎返回 ctx 自身是既定契约，任何 clear()+update(eng_ret[x]) 模式都要先判同一性。
- [2026-09-10 09:50:24] [2026-09-10 09:49] v5.1.2「设置面板所有模型消失」事故已修复+根治加固：根因=并行会话 9:20:42 用 PowerShell `ConvertTo-Json | Set-Content -Encoding UTF8` 重写 public/config/models.json 添加 GPT-6 Astra 模型，PS5.1 的 UTF8 必带 BOM(EF BB BF)，服务器 model_config._load_json 用 encoding='utf-8'+json.load 遇 BOM 抛 JSONDecodeError 被 `except: return default` 静默吞掉 → /api/models/config 返回 0 模型 → 前端 Models.list 空 + 发消息报「⚠️ 原模型配置不存在」。修复=字节级剥离 BOM（备份 public/config/models.json.bom.bak）+ 5 个 models.json 读取点全部改 encoding='utf-8-sig'（model_config._load_json、chat_gate._resolve_model_for 探测、zf3d_heartbeat 心跳上传、tools/video_gen_engine、config_agent 插件 read_models.py；vision_pick/image_gen 本就走 load_models_config 自动覆盖；write_models 的字节级备份读取故意保留 utf-8），09:49 重启(PID 9908)生效，e2e 实测带 BOM 文件返回 14 模型（修复前=0）、还原 MD5 一致。**Why:** 本机常有多个 AI 会话并行用 PowerShell 改 JSON 配置，PS5.1 UTF8=BOM 是系统级陷阱，_load_json 吞异常让损坏静默化。**How to apply:** ①本机改 JSON 配置一律走服务器 API/Python 写盘，用 PowerShell ConvertTo-Json 写盘后必须检查头 3 字节不是 EF BB BF；②诊断「模型全没了/下拉框空」先 curl /api/models/config 看服务端实际返回数（前端只是显示层）；③教训：对生产配置文件做字节手术类测试极易自伤（当日回归脚本变量名写错致 models.json 被 finally 剥掉`{`，靠 .bom.bak 精确还原）——此类验证一律用临时文件，不动生产配置。
- [2026-09-10 12:22:13] [2026-09-10 12:20] v5.1.2「所有 POST 全 500」事故已修复并实测通过：并行会话给 handler_base.py 的 _read_body 加 lone-surrogate 清洗（_sanitize_surrogates，\ud800-\udfff→'?'，防 SQLite UnicodeEncodeError）时把递归误写成 MixinBase._sanitize_surrogates——MixinBase 是 routes/mixin_base.py 的空壳类，与 handler_base.py 的 HandlerBase 无关——且已 commit 并热重载进运行中服务器（12:06 起 /api/db、/api/proxy、全部工具调用 500）。修复=两行改回 HandlerBase，12:15:39 热重载自动生效无需重启，e2e（POST /api/db/kv 带 \ud83d）实测 ok:true。对话侧症状补全（独立复核）：mixin_chat_pool/mixin_proxy_stream 等代理路由均为 `try: _read_body() except: body={}`，同一故障被吞成空body后过不了 _target_url 校验 → 12:08-12:15 所有对话发送报 400「Missing _target_url」风暴（用户报告 cb155，智谱/火山全中，前端无辜，重试重建上下文无效）；判据=POST /api/proxy 无 _target_url 看返回的 PROXY_DIAG：body_keys=[] 即 _read_body 死、有keys 即活。修复后实测 body_keys 完整解析（proxy/pool 共用同一 _read_body）、pool enabled、gate 6车道正常。同源：mixin_settings.py 已拆分为 6 个 mixin_settings_*.py（11:50 热重载曾因 updater 被截断失败，后并行会话自行补全；已验证拆分零方法丢失、compileall 全过、11 个关键模块 import OK，重启安全）。已知残留缺口：_read_json() 与部分 _cached_body 直读路径（如 mixin_settings_updater 的 repair_ack）不走 sanitize，同类 UnicodeEncodeError 仍潜伏；节点 title 里的坏 emoji 保存后会变 '?'（源头是前端字符串截断产生孤立代理项，不可逆）。**Why:** 多会话并行改核心文件且带病 commit+热重载，破坏直接进运行中服务器。**How to apply:** ①「所有 POST 全 500」或「对话全报400 Missing _target_url」先查是否有人在改 handler_base.py（_read_body 是全部 POST 必经之路）；②改热重载范围内 py 文件（成功无日志、失败才打❌、数秒生效）后必须立即 curl 一发真实 POST 验证；③警惕 HandlerBase(handler_base.py) vs MixinBase(routes/mixin_base.py 空壳) 命名陷阱。
- [2026-09-10 12:22:17] [2026-09-10 06:36:50] [2026-09-10 06:40] chat_pool v1.0「全请求头丢失→上游401」事故已修复（06:36重启PID 8360生效）：根因=pool_worker._engine_preprocess 中 `headers.clear(); headers.update(eng_ret['headers'])`——zf_core 引擎返回的就是传入 ctx 本身，eng_ret['headers'] 与 headers 同一对象，clear() 把自己清空→Authorization 全丢→池路径所有请求无鉴权头→火山401"missing or invalid"/智谱401"未收到Authorization参数"。旧路径(mixin_proxy L199)用变量重绑定 `headers=_eng_ret['headers']` 无此问题。修复=合并前同一性判断（`_ret_headers is not headers` 才 clear+update，payload 同理），备份 pool_worker.py.bak.20260910_064800_401fix。**Why:** 昨晚池 e2e 全打 127.0.0.1 mock（不校验 Authorization），头丢失零暴露；今早首批真实 ark 流量 100% 401（c63/9jk/lzl 三个turn），key 无辜（ark-...13ec5 直 curl 200 有效）。**How to apply:** ①改池/代理层后必须用真实上游（或校验 Authorization 的 echo server）做一次冒烟；②诊断 401 先判"头到底有没有到上游"：Ark 报"missing or invalid"兼容头缺失，用同一掩码key分别打旧 /api/proxy 与 /api/chat-pool/chat 可秒判哪条路径坏；③引擎返回 ctx 自身是既定契约，任何 clear()+update(eng_ret[x]) 模式都要先判同一性。（注：本条目于 12:18 记忆更新时被误删，12:22 恢复）
- [2026-09-10 12:22:20] [2026-09-10 09:50:24] [2026-09-10 09:49] v5.1.2「设置面板所有模型消失」事故已修复+根治加固：根因=并行会话 9:20:42 用 PowerShell `ConvertTo-Json | Set-Content -Encoding UTF8` 重写 public/config/models.json 添加 GPT-6 Astra 模型，PS5.1 的 UTF8 必带 BOM(EF BB BF)，服务器 model_config._load_json 用 encoding='utf-8'+json.load 遇 BOM 抛 JSONDecodeError 被 `except: return default` 静默吞掉 → /api/models/config 返回 0 模型 → 前端 Models.list 空 + 发消息报「⚠️ 原模型配置不存在」。修复=字节级剥离 BOM（备份 public/config/models.json.bom.bak）+ 5 个 models.json 读取点全部改 encoding='utf-8-sig'（model_config._load_json、chat_gate._resolve_model_for 探测、zf3d_heartbeat 心跳上传、tools/video_gen_engine、config_agent 插件 read_models.py；vision_pick/image_gen 本就走 load_models_config 自动覆盖；write_models 的字节级备份读取故意保留 utf-8），09:49 重启(PID 9908)生效，e2e 实测带 BOM 文件返回 14 模型（修复前=0）、还原 MD5 一致。**Why:** 本机常有多个 AI 会话并行用 PowerShell 改 JSON 配置，PS5.1 UTF8=BOM 是系统级陷阱，_load_json 吞异常让损坏静默化。**How to apply:** ①本机改 JSON 配置一律走服务器 API/Python 写盘，用 PowerShell ConvertTo-Json 写盘后必须检查头 3 字节不是 EF BB BF；②诊断「模型全没了/下拉框空」先 curl /api/models/config 看服务端实际返回数（前端只是显示层）；③教训：对生产配置文件做字节手术类测试极易自伤（当日回归脚本变量名写错致 models.json 被 finally 剥掉`{`，靠 .bom.bak 精确还原）——此类验证一律用临时文件，不动生产配置。（注：本条目于 12:18 记忆更新时被误删，12:22 恢复）
- [2026-09-10 12:46:24] [2026-09-10 12:47] 朱峰v5.1.2「工具轮后第一个聊天请求400 Missing _target_url」诊断状态：①【环境剧变】server_stdout.log自10:43:59失联——当前服务器PID 16124是10:37:51由启动bat拉起（stdout=bat隐形控制台窗口，不写日志）；10:43:59前的日志内容来自09:49 restart_server启动的PID 9908。今后排障禁止依赖server_stdout.log看"当前"，一律查SQLite app_logs+新探针文件。②【热重载缺口】155个监视py不含routes/mixin_chat_pool.py和chat_pool.py——池模块改动无法热重载，必须重启才生效（昨天池上线时漏登记；_pool400_diag no-target探针已写进mixin但重启才激活）。③【已布防】handler_base.py _read_body异常路径→server/diag_body_400.log（[BodyDiag] READ-EX/EMPTY-BODY），12:45热重载验证生效，正常请求零开销。④【病灶事实】cb156三次（12:21/12:27/12:33）均在工具轮后的首个聊天请求400→前端重建循环；重建后的干净载荷也连败3次、同载荷第5次成功=瞬态故障非载荷内容；脚本复现78请求（含池turn+SSE订阅+工具全序列、大小载荷）不复发，疑Edge连接池并发态。⑤【判读】下一次复现看diag_body_400.log：READ-EX行=读体异常（连接中途断/编码坏）；EMPTY-BODY行=无Content-Length或chunked；无行但前端报400=body解析成功但真缺_target_url（前端载荷构造问题）。

### Reference

