# ZF3D Agent v2 — 项目描述

## 根目录
项目当前工作目录

## 项目简介
这是一个全JSON配置驱动的AI智能体框架。系统是手脚（调度、搬运、执行），大模型是大脑（思考、决策、优化），用户是主人（授权、审批、合并）。

## 技术栈
- Python 3.8+（纯标准库，零外部依赖）
- 前端：纯HTML/CSS/JavaScript（无框架）
- LLM接口：兼容OpenAI格式（通过urllib直连，不依赖SDK）
- 存储：JSON文件 + SQLite（FTS5全文搜索，对话记录结构化存储与检索）

## 目录结构
```
项目根目录/
├── CLAUDE.md                 ← 项目描述（自动注入AI上下文）
├── 公共区/                    ← 🔵 公开区域，可发布可分享
│   ├── 内核/                  ← 系统引擎（19个组件 + 操作/24个文件）
│   │   ├── 启动器.py          ← 读取配置→加载配置→注册操作→加载技能→连接MCP→启动Web
│   │   ├── 配置加载器.py      ← JSON加载+热重载，全局事件中心+全局命令中心
│   │   ├── 模型直连器.py      ← 纯HTTP直连LLM，密钥加密，响应缓存，Token统计，流式输出
│   │   ├── 网页服务.py        ← http.server，Web界面+API接口
│   │   ├── 文件管理器.py      ← 权限校验+3次放行+回收站+审计日志+移动/复制
│   │   ├── 操作注册中心.py    ← 注册/调度/执行120个操作，中英文映射，模糊匹配，统计
│   │   ├── 操作基类.py        ← 操作抽象基类+操作结果
│   │   ├── 安全计算器.py      ← AST安全求值，零eval注入风险
│   │   ├── 存储引擎.py        ← SQLite+FTS5，对话记录结构化存储与全文搜索
│   │   ├── 运行诊断器.py      ← 运行时异常捕获+监控规则引擎
│   │   ├── 剧本管理器.py      ← 剧本录制与回放
│   │   ├── 技能加载器.py      ← 扫描技能目录，解析SKILL.md，脚本包装为操作
│   │   ├── 插件加载器.py      ← importlib动态加载插件
│   │   ├── 动态工具加载器.py  ← 从JSON声明生成工具（http/command/python三种类型）
│   │   ├── MCP客户端.py       ← 连接外部MCP Server，自动发现工具并注册
│   │   ├── 更新检查器.py      ← GitHub/Gitee多源自动更新检测
│   │   ├── 系统托盘.py        ← 系统托盘图标
│   │   ├── 定时任务与影响分析.py ← 定时任务调度器
│   │   ├── 股票缓存.py        ← 股票数据缓存
│   │   └── 操作/              ← 操作类拆分（24个文件）
│   │       ├── 文件.py / 代码.py / Git.py / 系统.py / 网络.py
│   │       ├── 高级.py / 浏览器操作.py / 诊断.py
│   │       ├── ComfyUI操作.py / Word.py / Excel.py
│   │       ├── 图片处理.py / 图片下载.py / 压缩.py
│   │       ├── 记忆操作.py / 剧本操作.py / 账本操作.py
│   │       ├── 知识库操作.py / 股票预测.py / 询问用户.py
│   │       ├── Job.py / 导出与配置操作.py / 多线程下载.py / 基类.py
│   │       └── ...
│   ├── 模块/                  ← 功能模块
│   │   ├── 对话/              ← ReAct循环+流式输出+多对话+经验师+意图解析+上下文管理
│   │   │   ├── 主程序.py / 推理引擎.py / 意图解析器.py
│   │   │   ├── 上下文管理器.py / 提示词构建器.py / 反思评估器.py
│   │   │   ├── 任务规划器.py / 经验师.py
│   │   │   └── 模块声明.json
│   │   ├── 任务/              ← Plan-and-Execute + 子代理 + 并行/流水线
│   │   ├── 记忆/              ← 3层语义记忆 + 自动摘要 + 用户画像
│   │   └── 浏览器/            ← Playwright封装 + 页面分析 + 会话管理
│   ├── 插件/                  ← 可插拔扩展（放入即启用）
│   ├── 技能/                  ← 技能包（SKILL.md + 脚本 + 参考文档）
│   ├── 配置/                  ← 全JSON配置（13个文件）
│   └── 界面/                  ← 前端页面（主页 + 编辑器 + 图片加工工具 + 去背景工具）
├── 隐私区/                    ← 🔴 隐私区域，绝不进发布包
│   ├── 我的配置/              ← 密钥（加密）+ 询问记录
│   ├── 我的记忆/              ← 记忆库 / 用户画像 / 摘要索引
│   ├── 我的数据/              ← 用户个人数据（SQLite数据库）
│   ├── 我的日志/              ← 运行日志（含LLM原始请求响应）
│   ├── 对话记录/              ← 对话历史
│   ├── 浏览器会话/            ← 浏览器Cookie/缓存
│   ├── 我的工作引擎/          ← 开发版引擎（大模型实验区）
│   ├── 我的经验/              ← 经验师自动提炼的经验文档
│   ├── 我的剧本/              ← 用户剧本
│   └── 我的知识库/            ← 个人知识库
├── 引擎管理/                 ← 双引擎调度与合并控制
├── tests/                    ← pytest测试
└── public/core/              ← ASCII安全入口（bat兼容中文路径）
```

## 核心架构
```
大模型(大脑) ←→ 全局层(命令中心+事件中心) ←→ 用户层(Web界面)
                       ↕
                   配置层(全JSON)
                       ↕
        ┌──── 内核(极小，仅调度) ────┐
        ├ 启动器 / 配置加载器        │
        ├ 模型直连器 / 文件管理器     │
        ├ 操作注册中心 / 网页服务     │
        ├ 安全计算器 / 存储引擎       │
        ├ 运行诊断器 / 技能/插件/MCP  │
        └────────────────────────────┘
           ↕        ↕        ↕
        [对话模块] [任务模块] [记忆模块]
```

## 关键规则
1. **全中文命名** — 函数、变量、文件夹、配置全中文
2. **零依赖** — 不pip install任何包，纯标准库
3. **相对路径优先** — 所有路径使用相对路径，复制到任何位置都能跑
4. **文件权限铁律** — 操作新文件夹必须弹窗询问，无权限绝对不动
5. **隐私分区** — 隐私区绝不进入发布包，一键打包只打公共区
6. **大模型全权决策** — 系统不自己设计思路，所有问题丢给大模型
7. **用户授权** — 用户已授予AI对本项目根目录及所有子目录/文件的永久读写权限，不需要再弹窗询问

## 内置操作（120个）
- **文件操作**：创建/读取/写入/追加/删除/替换文本/批量编辑/列出目录/移动/复制/回收站管理
- **代码操作**：搜索代码(正则)/Glob搜索/符号搜索(AST)/验证代码(语法检查)/自动测试/构建验证
- **Git操作**：状态/提交/回滚/差异/日志/分支
- **系统操作**：打开程序/运行命令/截图/获取时间/系统信息/等待/数学计算/JSON操作
- **网络操作**：网页抓取/网络搜索/网页分析(LLM)/图片分析(LLM Vision)/下载网页图片/多线程下载
- **AI生成(ComfyUI)**：一键生图/图片修改/视频生成/反推/工作流管理/模型列表/队列控制/上传图片等14个
- **浏览器(Playwright)**：打开网页/截图/点击元素/填写表单/滚动/页面内容读取/会话管理等14个
- **Office文档**：Word读取/修改/创建/段落操作/Excel文本替换
- **图片处理**：去水印/去杂物/调整/裁剪/缩放/模糊/灰度化/旋转(8个)
- **任务编排**：子代理/并行执行/Pipeline/Barrier/LoopUntilDry/Job管理/后台执行/查Bug/编程循环
- **诊断监控**：查询/解决/清除运行错误/监控规则管理/搜索操作结果
- **其他**：剧本录制回放/知识库导入搜索/记忆操作/账本/询问用户/TTS语音/对话导出/股票预测

## 启动方式
- Windows: 双击 `启动.bat` 或 `python 公共区/内核/启动器.py`
- 浏览器打开 http://localhost:8765
