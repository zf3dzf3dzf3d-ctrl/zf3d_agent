# 加密发布指南（给下一个AI看）

## 目标
发布版本时对核心文件加密/混淆，保护源代码，用户使用无感知差异。

## 需要加密的文件

### Python核心（PyArmor加密）
1. `公共区/内核/模型直连器.py`
2. `公共区/模块/对话/提示词构建器.py`
3. `公共区/模块/对话/推理引擎.py`
4. `公共区/模块/对话/反思评估器.py`
5. `公共区/模块/对话/任务规划器.py`
6. `公共区/内核/网页服务.py` — 🔒含API路由（签到/员工备份恢复/网站认证等敏感接口）
7. `公共区/内核/操作/Git.py` — 🔒含Git操作（push/pull/stash/tag，防止篡改）
8. `公共区/内核/操作/代码.py` — 🔒含代码分析操作（AST分析/符号引擎调用）
9. `公共区/内核/操作/高级.py` — 🔒含子代理/编程循环（防止绕过安全限制）
10. `公共区/内核/操作/网络.py` — 🔒含网络操作（网页抓取/API调用）

### 前端核心（JS混淆）
10. `公共区/界面/员工浮窗.js` — 🔒含员工备份恢复/分组持久化逻辑
11. `公共区/界面/模块/网站登录.js` — 🔒含签到API调用/登录状态管理
12. `公共区/界面/模块/动画工坊.js` — 🔒含VIP校验逻辑/动画生成API调用（VIP专属功能保护）

### 不加密的文件
- 文件管理器.py、操作注册中心.py、启动器.py 等框架文件
- 操作类（公共区/内核/操作/*.py）中非敏感文件（文件.py/系统.py/网络.py等）
- 前端 HTML/CSS/其他JS模块
- 所有JSON配置文件（用户可自定义，不应加密）

### 加密优先级说明
- **必须加密**（含API密钥/网站认证/安全敏感逻辑）：网页服务.py、网站登录.js、模型直连器.py
- **强烈建议加密**（含核心业务逻辑）：推理引擎.py、提示词构建器.py、员工浮窗.js
- **建议加密**（含操作实现）：Git.py、代码.py、高级.py、反思评估器.py、任务规划器.py

## 实现步骤

### 步骤1：安装工具

```bash
# PyArmor — Python代码加密
pip install pyarmor

# javascript-obfuscator — JS混淆
npm install -g javascript-obfuscator
```

### 步骤2：修改 发布打包.py

在发布打包.py 中新增加密流程，在"复制公共区文件"之后、"隐私泄露检查"之前执行：

```python
def 加密核心文件(发布目录):
    """发布前加密核心文件"""
    import subprocess, shutil, os

    # === PyArmor加密5个Python文件 ===
    py核心文件 = [
        "公共区/内核/模型直连器.py",
        "公共区/模块/对话/提示词构建器.py",
        "公共区/模块/对话/推理引擎.py",
        "公共区/模块/对话/反思评估器.py",
        "公共区/模块/对话/任务规划器.py",
        "公共区/内核/网页服务.py",          # 🔒API路由（签到/备份恢复/网站认证）
        "公共区/内核/操作/Git.py",          # 🔒Git操作（push/pull/stash/tag）
        "公共区/内核/操作/代码.py",         # 🔒代码分析操作
        "公共区/内核/操作/高级.py",         # 🔒子代理/编程循环
        "公共区/内核/操作/网络.py",         # 🔒网络操作（网页抓取/API调用）
    ]

    for 文件 in py核心文件:
        发布路径 = os.path.join(发布目录, 文件)
        if not os.path.exists(发布路径):
            print(f"  ⚠️ 跳过（不存在）: {文件}")
            continue

        # PyArmor加密（输出覆盖原文件）
        print(f"  🔒 加密: {文件}")
        结果 = subprocess.run([
            "pyarmor", "gen",
            "--output", os.path.dirname(发布路径),
            "--replace",
            发布路径
        ], capture_output=True, text=True, encoding='utf-8')

        if 结果.returncode != 0:
            print(f"  ❌ 加密失败: {文件}")
            print(f"     {结果.stderr[:200]}")
            return False

    # === JS混淆 员工浮窗.js + 网站登录.js ===
    js文件列表 = [
        "公共区/界面/员工浮窗.js",            # 🔒员工备份恢复/分组持久化
        "公共区/界面/模块/网站登录.js",       # 🔒签到API/登录状态管理
    ]
    for js文件 in js文件列表:
        发布路径 = os.path.join(发布目录, js文件)
        if not os.path.exists(发布路径):
            print(f"  ⚠️ 跳过（不存在）: {js文件}")
            continue
        print(f"  🔒 混淆: {js文件}")
        备份路径 = 发布路径 + ".bak"
        shutil.copy2(发布路径, 备份路径)

        # javascript-obfuscator混淆
        结果 = subprocess.run([
            "javascript-obfuscator", 发布路径,
            "--output", 发布路径,
            "--compact", "true",
            "--control-flow-flattening", "true",
            "--control-flow-flattening-threshold", "0.75",
            "--dead-code-injection", "true",
            "--dead-code-injection-threshold", "0.4",
            "--string-array", "true",
            "--string-array-encoding", "base64",
            "--string-array-threshold", "0.75",
            "--identifier-names-generator", "hexadecimal",
            "--rename-globals", "false",
            "--self-defending", "true",
            "--reserved-names", "require,exports,module,define,amd",
            "--reserved-strings", "require\\(','exports','module'"
        ], capture_output=True, text=True, encoding='utf-8')

        if 结果.returncode != 0:
            print(f"  ❌ JS混淆失败: {js文件}")
            print(f"     {结果.stderr[:200]}")
            # 混淆失败时恢复原始文件
            shutil.copy2(备份路径, 发布路径)
            os.remove(备份路径)
            return False

        # 混淆成功，删除备份
        os.remove(备份路径)

    print("  ✅ 核心文件加密完成")
    return True
```

### 步骤3：调用时机

在 发布打包.py 的主流程中，找到"复制公共区文件"完成后、"隐私泄露检查"之前，插入：

```python
# 加密核心文件
print("\n🔒 加密核心文件...")
if not 加密核心文件(发布目录):
    print("❌ 加密失败，终止打包")
    return
```

### 步骤4：EXE打包也要加密

打包exe.py 中同理，在PyInstaller打包前对核心文件执行加密。
注意：PyArmor加密后的文件可以直接被PyInstaller打包，无冲突。

### 步骤5：开发版不加密

- 开发版（新系统_v2_开发版）永远保持明文
- 加密只作用于发布版（新系统_v2）和EXE打包
- 发布打包.py 从开发版复制到发布版后才执行加密

## 注意事项

1. **PyArmor试用期**：PyArmor免费版有功能限制（无超时、无授权绑定），商业版可选。免费版对基本混淆足够。
2. **JS混淆注意保留全局函数名**：员工浮窗.js中有些函数被HTML直接onclick调用，混淆时必须保留这些名称。用 `--reserved-names` 参数处理。需要检查哪些函数名被HTML直接引用。
3. **测试**：加密后必须启动发布版验证所有功能正常，特别是：
   - 对话功能（推理引擎+提示词构建器）
   - 节点工作流编辑器（员工浮窗.js）
   - 反思/导师介入功能（反思评估器.py）
   - 任务规划功能（任务规划器.py）
   - 模型连接（模型直连器.py）
4. **回退**：如果加密导致功能异常，发布打包.py应跳过加密继续打包（明文也能发布，只是不保护源码）
5. **PyArmor版本兼容**：PyArmor 8.x 用 `pyarmor gen` 命令；7.x 用 `pyarmor obfuscate` 命令。安装后先检查版本。

## 被HTML直接引用的JS函数名（混淆时必须保留）

需要检查 主页.html 和 员工管理.html 中所有 onclick="xxx()" 和 直接调用的 员工浮窗.js 函数名，
用 `--reserved-names` 参数传给 javascript-obfuscator。

### 员工浮窗.js 保留函数名（通过 window.empWidget 暴露）
init, togglePanel, refresh, selectEmployee, openChat, closeChat, sendChat, addEmployee,
editEmployee, deleteEmployee, clearChat, deleteMessage, showContextMenu, closeContextMenu,
setBossPrompt, clearSuperiors, clearSubordinates, openEditPanel, saveEdit, openCreatePanel,
saveCreate, generatePersona, openWorkflow, closeWorkflow, toggleWorkflow, clearWorkflow,
executeWorkflow, saveWorkflow, exportWorkflow, importWorkflow, saveWorkflowFile,
saveWorkflowAs, loadWorkflowFile, fitWorkflow, autoLayout, autoDesign, createWfFrame,
saveEmpBackup, restoreEmpBackup, wfUndo, wfRedo

### 网站登录.js 保留函数名（通过 window.agentAuth 暴露）
init, doLogin, showLogin, hideLogin, logout, requireLogin, showUserMenu, checkin,
isLoggedIn, getUser

### 动画工坊.js 保留函数名（通过 window.lottieWorkshop 暴露）
open, close, generate, exportHTML, saveToFolder, importFile, togglePlay, setSpeed,
cycleBg, startNew, startModify

### 检查方法
```bash
# 找出HTML中直接调用员工浮窗.js函数的地方
grep -oP 'onclick="[^"]*\w+\([^)]*\)"' 主页.html 员工管理.html
```

## 时间估算

| 步骤 | 耗时 |
|------|------|
| 安装PyArmor+javascript-obfuscator | 5分钟 |
| 写加密函数代码 | 15分钟 |
| 找出需要保留的JS函数名 | 10分钟 |
| 集成到发布打包.py | 10分钟 |
| 测试加密后功能 | 20分钟 |
| **总计** | **约1小时** |
