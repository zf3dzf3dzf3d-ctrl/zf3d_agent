# 你的模式名（模板）

在此写该对话模式的系统提示词。

建议结构：
1. **角色定义**：这个模式里 AI 是谁、负责什么
2. **职责清单**：能做什么、不能做什么
3. **工具使用规则**：每个工具的调用时机与参数说明
4. **安全与边界**：禁区（不碰的文件/操作）、失败时的表现
5. **回复风格**：语言、格式要求

可用占位符：
- `{PROJECT_ROOT}` → 当前对话关联项目的真实路径
- `{BASE_ROOT}` → 软件安装根目录
