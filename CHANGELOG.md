# 更新日志

## v3.5.0 (2026-07-25)

### 🧠 神经元对话系统全面优化

- **SSE心跳保活** — 长对话每5秒发送keep-alive，防止代理/浏览器超时断连
- **流式状态指示** — LLM响应时立即显示"🧠 思考中"和"🔧 调用工具: xxx"，不再空白等待
- **回复就绪事件** — AI回复文本先于后处理（经验师/绕路师等）显示到前端，无需等待10-20秒
- **上下文记忆增强** — 历史从20→30条、保留8条、摘要600字、摘要必须保留路径/版本/配置信息
- **关键信息自动提取** — 每轮对话后自动用正则提取路径/版本号/GPU信息/ComfyUI路径写入SQLite
- **项目记忆文档** — 工作目录下自动维护.ai记忆.md，记录路径/环境/目录结构/测试结果/修改文件/用户需求
- **任务跟踪系统** — 用户确认任务状态（✅已完成/⏳待确认/🗑️放弃任务），只在AI做了写入操作后才弹
- **三种执行模式** — 自动编辑（优化/修改/修复）/编辑前询问（删除/覆盖）/计划模式（分析/方案），关键词自动判断
- **可点击选项按钮** — AI回复中的选项列表自动渲染为可点击按钮，点击后自动发送
- **控制台日志复制** — 控制台新增📋复制按钮，一键复制全部日志
- **工作目录固定** — 打开文件时工作目录不再跳变，记住首次设置
- **run_command护栏放宽** — 从5次→8次阻断，避免git init+add+status等正常多步操作被拦

### 🐛 Bug修复

- **DeepSeek模型名更新** — deepseek-chat已废弃，改为deepseek-v4-flash（API返回400修复）
- **CSS变量未定义** — --text1/--text3变量添加到:root，14处文字颜色恢复正常
- **neuron CSS语法错误** — 删除多余的}，body选择器改为#app避免覆盖全局字体
- **ReAct卡片残留** — 第二次ReAct不显示的bug，开始时清除上次残留卡片
- **状态指示器覆盖** — "思考中"状态不再覆盖assistantMsg内容，改为独立临时元素
- **Markdown渲染遗漏** — 操作确认弹窗和理解润色师提问改用renderMarkdown
- **写入检测误触发** — 提问标记50+同义词全消息扫描+问号检测，训练输出"随机删除"排除
- **写入检测循环** — 成功验证检查（含True/成功/✅时跳过）+限制前5轮触发
- **任务确认无限循环** — 按钮改为静默标记不sendMessage
- **正则语法错误** — 路径提取正则改用三引号字符串
- **帮助系统.md格式** — 8个格式问题修复（空白行/重复编号/内容矛盾等）

### 🎨 界面改进

- **ReAct节点图** — 标准布局（所有行左→右）+同行贝塞尔连线+换行不画连线
- **命令卡片归位** — run_command/run_test/verify_code卡片显示在ReAct日志区内部
- **统计栏精简** — 只显示4项：⚡步数 ⏱时间 🔤token 💰费用，去掉明细行
- **代码块复制按钮** — 透明度从0.5提到0.6，hover时高亮背景
- **神经元面板** — 移除"X个神经元"数量标注

---

## v3.4.0 (2026-07-24)

### 🖼️ 图片查看器增强

- **Delete键删除图片** — 大图查看模式下按Delete键可直接删除当前图片，弹出确认对话框防止误删，删除后自动跳转下一张，全部删除后自动返回画廊

---

## v3.3.8 (2026-07-16)

### 🎬 动画工坊 — 一句话生成SVG矢量动画（VIP专属）

- **一句话生成** — 输入文字描述，AI自动生成SVG/HTML矢量动画代码，支持尺寸/时长/循环/格式设置
- **导入修改** — 导入已有.svg/.html动画文件，输入修改需求让AI在原代码基础上调整，支持连续修改
- **实时预览控制** — 播放/暂停按钮 + 0.25x~3x速度调节滑块，逐帧观察动画细节
- **背景切换** — 深色/白色/棋盘格三种背景一键切换，方便查看透明效果
- **一键保存** — 保存到当前文件夹或另存为独立文件，格式自动匹配
- **可拖拽对话框** — 标题栏拖拽移动 + 8方向边缘缩放，内容区跟随缩放
- **VIP权限控制** — 前端校验+后端二次校验，未开通/过期弹窗引导购买，代码混淆加密保护

---

## v3.3.7 (2026-07-15)

### 🎬 动画工坊 — 一句话生成SVG矢量动画（VIP专属）

- **一句话生成** — 输入文字描述，AI自动生成SVG/HTML矢量动画代码，支持尺寸/时长/循环/格式设置
- **导入修改** — 导入已有.svg/.html动画文件，输入修改需求让AI在原代码基础上调整，支持连续修改
- **实时预览控制** — 播放/暂停按钮 + 0.25x~3x速度调节滑块，逐帧观察动画细节
- **背景切换** — 深色/白色/棋盘格三种背景一键切换，方便查看透明效果
- **一键保存** — 保存到当前文件夹或另存为独立文件，格式自动匹配
- **可拖拽对话框** — 标题栏拖拽移动 + 8方向边缘缩放，内容区跟随缩放
- **VIP权限控制** — 前端校验+后端二次校验，未开通/过期弹窗引导购买，代码混淆加密保护

---

## v3.3.6 (2026-07-15)

### 🚀 后台任务系统 v2

- **实时进度查看** — 后台任务输出持久化到文件，新增「查看后台进度」操作可随时查看末尾N行输出，不再是个黑盒
- **停止后台任务** — 新增「停止后台任务」操作，运行命令类任务会终止子进程
- **完成自动通知** — 前端自动轮询后台任务状态，任务完成/失败/停止时弹Toast通知，无需手动查询
- **获取结果增强** — running状态也返回最近10行输出，掌握实时进度

---

## v3.3.5 (2026-07-15)

### 🔀 节点工作流并发修复

- **同层节点延迟启动** — 并行分支的节点逐个延迟50ms启动，避免所有节点同时请求LLM API导致 `连接错误: Idle`。涉及3处并发执行点（普通模式+循环模式前后层），20个节点同时执行时最后一个延迟约1秒，对LLM调用几乎无影响

### 📊 智能体每日统计系统

- **启动心跳上报** — 智能体每次启动自动向网站上报（MAC哈希匿名机器ID、版本号、登录状态、用户名），后台静默执行不阻塞启动
- **网站统计API** — `agent_api.asp` 新增 `heartbeat`（接收心跳，按 machine_id+date 去重）和 `daily_stats`（查询每日统计）两个接口
- **管理页统计面板** — `guanliy.asp` 新增智能体板块：今日活跃设备(去重)/已登录/未登录/总启动次数4个卡片 + 近7天柱状图

---

## v3.3.2 (2026-07-13)

### 🧠 AI推理质量大幅提升

- **防幻觉硬约束** — 回答代码位置/实现类问题时，系统自动执行搜索代码并注入结果，AI基于搜索结果回答而非凭记忆猜测
- **防循环引擎重构** — 7层防循环检测体系（搜索结果去重→搜索频率→只读类操作合并→搜索总次数上限→非幂等频率→连续阻断→死循环），彻底消除搜索死循环和列目录死循环
- **回复排版规范** — 系统提示词新增Markdown排版规范+沟通纪律，AI回复从平铺式改为结构化（表格+标题+加粗+列表）
- **思考原则** — 新增6条思考原则（先给结论/不铺垫/不扩展/不重复/短句优先/精简执行）

### 🔒 安全修复

- **路径穿越防护** — 网页服务静态文件解析改用resolve()+relative_to()校验，堵住绝对路径绕过漏洞
- **请求体大小限制** — POST请求体上限100MB，防止OOM攻击
- **save-image端点加固** — 路径校验+50MB大小限制
- **API密钥脱敏** — 模型直连器返回值中Authorization/X-API-Key统一掩码（8处）
- **文件管理器授权边界** — startswith前缀匹配改为os.sep边界检查，防止路径前缀误匹配

### ⚡ 性能优化

- **max_tokens 8192→4096** — 所有20个模型统一降低，减少冗长回复+加速响应
- **最大推理步数 200→80** — 避免长任务卡死
- **超时秒数 120→300** — 从配置读取而非硬编码，复杂任务不再频繁超时
- **里程碑提醒提前** — 第8步开始提醒收敛（原15步）
- **向量搜索加LIMIT 500** — 避免全表扫描
- **文档频率60秒缓存** — 避免每次插入都全表扫描

### 🔧 稳定性修复

- **推理引擎死锁修复** — _推入推理流回调移出锁外，避免回调间接调用获取推理流()死锁
- **步数回退移除** — 重试不再步数-=1，防止绕过最大步数限制
- **静默异常加日志** — 4处except:pass改为打印警告
- **临时文件自动清理** — 超过1小时的cmd_output临时文件自动删除
- **记忆模块并发修复** — 删除重复线程、自动召回加锁、摘要索引500上限、归纳重入保护
- **上下文管理器压缩锁** — 重入保护生效，防止双重压缩
- **反思评估器5xx分类** — 500/502/503归类为网络错误（指数退避），限流恢复阈值2→4
- **配置加载器原子写入** — JSON写入改为临时文件+rename，防止崩溃损坏配置
- **启动器停止完整性** — 补全定时任务调度器+热重载线程关闭

### 🌐 前端修复

- **dev-reload生产环境禁用** — 开发模式热重载改为HEAD探测，非开发模式不轮询
- **SSE错误信息中文化** — 网络断开显示中文提示而非英文异常
- **推理流事件监听器泄漏** — 拖拽resize改为按需添加/移除监听器
- **网页抓取编码修复** — 自动检测charset（GBK/UTF-8），新浪财经等中文网站不再乱码

### 🐛 Bug修复

- **浏览器引擎变量名** — _cdp树转嵌套方法中cdp_tree（英文）改为cdp树（中文），修复"打开网页"操作NameError
- **存储引擎FTS5重复** — 记录绕路UPDATE分支不再重复插入FTS5索引

---

## v3.3.0 (2026-07-13)

### 🎉 重大新增：朱峰社区AIGC发布系统

- **AIGC发布节点** — 节点工作流新增"发AIGC文章"节点，支持AI图片/视频/游戏/软件四个分类，双击弹出表单填写标题/内容/封面，一键发布到朱峰社区
- **朱峰社区API对接** — 智能体与zf3d.com深度打通，支持发帖/发文章/发作品/发评论/作品评价/签到等23个API操作，密钥不暴露前端
- **发布流程** — ComfyUI出图→AI生成标题和介绍→自动填入发布表单→确认发布，全流程节点化
- **封面上传** — 本地图片路径自动上传到网站获取URL，支持远程图片URL自动下载
- **用户身份** — 发布内容自动记录当前登录用户的user_id，列表页显示作者头像和用户名

### 📦 模板系统

- **官方模板** — 管理员保存工作流模板（含员工+分组），随发布包分发给所有用户。普通用户加载官方模板即可还原完整工作流，零配置上手

### 🔧 编程能力大幅增强

- **Function Calling修复** — 所有模型的FC功能之前被禁用（读错配置键），现已修复，DeepSeek等18个模型支持FC
- **Git操作安全化** — 5个Git操作从shell=True改为列表参数，消除命令注入风险
- **新增4个Git操作** — push/pull/stash/tag
- **多语言代码验证** — 支持JS/TS/HTML/CSS/XML/YAML/Shell语法检查
- **符号引擎扩展** — 支持TS/Go/Rust/Java/C#/C++等12种新语言的符号解析
- **AST代码分析** — 未使用导入/函数检测、圈复杂度、最大嵌套深度
- **代码差异操作** — difflib生成unified diff
- **编程行为准则** — 系统提示词新增编程规范
- **步数限制** — 最大推理步数从999改为200，防止死循环

### 🔒 发布加密

- **PyArmor加密** — 5个核心Python文件用PyArmor加密
- **compile混淆** — 5个大文件用AST+marshal+zlib混淆（绕过试用版限制）
- **10个文件全部加密** — 模型直连器/推理引擎/网页服务/提示词构建器等

### 🛠️ 其他改进

- **员工备份恢复** — 一键备份员工+分组到文件，恢复时自动合并
- **员工分组持久化** — 分组数据存到后端文件，清缓存不丢
- **签到功能** — 用户菜单新增每日签到，已签到显示状态
- **Ctrl+Shift+R快捷重启** — 带进度动画，自动杀旧进程清缓存
- **多端口节点** — 发布节点支持标题/内容/封面三色输入端口
- **打印任何节点** — 调试用，双击显示上游完整数据可复制
- **Delete键删除员工** — 选中员工后按Delete删除
- **发布防重复** — 点击发布后锁定，转圈动画提示
- **Linux进程树修复** — start_new_session=True
- **上下文裁剪修复** — 孤儿tool消息导致API 400

## v3.2.1 (2026-07-10)

### 新增
- **面包屑路径栏** — 缩略图上方仿Windows资源管理器路径栏，点击文字进入文件夹、点击▸下拉子文件夹列表、点击✏输入路径回车跳转
- **顶栏网站入口** — 🌐图标一键访问朱峰社区网站(zf3d.com)
- **顶栏渐变动画** — 标题logo和文字蓝色渐变流动效果（与zf3d.com网站一致）

### 优化
- **左侧文件树持久化** — 树结构永远保持"我的电脑→盘符→子文件夹"，切换文件夹只移动高亮不重建树
- **面板拖拽隐藏** — 中间面板可拖到60px以下自动隐藏，恢复时还原初始大小（修复min-width卡死）
- **版本号动态显示** — 浏览器标题/顶栏/关于页面三处从API自动读取版本号，改配置一处全同步
- **顶栏布局调整** — 标题靠左，📁📝💬📊右侧分组，🔀🔊⚙️❤️最右侧，间距可调
- **定时任务防重复** — 工作流执行期间加锁，同一任务不会重复触发
- **通知精简** — 工作流成功时alert节点已弹过不再额外弹完成通知，失败时才弹
- **隐私安全加固** — 清除硬编码个人路径，MCP配置改为相对路径且默认关闭
- **垃圾文件清理** — 删除备份目录/build/旧包/临时文件/__pycache__
- **版本号统一** — 全局从3.1.1/3.0.2混用统一为3.2.1

### 修复
- **文件树导航15项bug** — 路径规范化/active管理/列表视图一致性/expandTreeNode静默失败/showGallery早期返回/async竞态/尾斜杠/goUp源不匹配等
- **文件管理器权限** — `System Volume Information`等无权限目录不再导致整个目录读取失败
- **MCP服务3dsmax默认关闭** — 避免新用户启动报错
- **驱动器列表** — 前端字段名与后端不匹配导致"无法获取驱动器列表"
- **路径输入框** — mousedown事件冲突导致无法输入/复制/粘贴

---

## v3.2.0 (2026-07-09)

### 新增
- **员工列表整合到工作流右侧栏** — 去掉独立气泡和聊天，员工列表固定在工作流面板右侧（可拖拽调宽）
- **21个操作节点** — 文件操作(4)/网络操作(3)/系统操作(2)/图片处理(4)/代码工具(2)/媒体(4) 全部零token
- **🔔弹窗提醒节点** — 零token，弹窗+Web Audio提示音
- **日历计划任务** — 替换简单调度为月历视图，支持仅一次/每日/每周/每月/间隔模式
- **任务修改功能** — ✏️编辑任务名称/时间/日期/重复模式
- **桌面通知** — 前端离线时弹Windows MessageBox置顶通知，前端在线时浏览器内弹窗+提示音
- **开发热重载** — 前端文件保存自动刷新，后端通过重启按钮重载
- **IndexedDB持久化文件目录** — 打开/另存为首次选目录后自动记住
- **关于页面** — 设置面板新增ℹ️关于标签页
- **顶栏🔀按钮** — 一键打开/关闭节点工作流

### 修复
- **节点拖拽** — 点击节点身体可拖拽（不阻止input/textarea焦点）
- **分组D键** — 点击分组清除节点选中，D键正确作用于分组
- **定时任务调度器** — 改用 `网页请求处理器._定时任务调度器` 直接引用
- **工作流文件子目录** — 支持搜索子目录中的工作流文件
- **文件拖入读取** — 浏览器外部拖入的文件直接用预读内容作为输出
- **连线错位** — 节点执行后结果栏改变高度，添加 `redrawWfConnections()` 重绘
- **保存失败** — `saveWorkflowFile` 添加 `覆盖: true` 参数
- **模型并发冲突** — 后台归纳用独立连接，不复用持久连接
- **`_http路径` 错误** — 非复用连接时自己解析URL路径

### 优化
- **启动加速** — 去掉banner/摘要sleep，跳过模型连通检查，Blender连接timeout 5→2秒，下载恢复改后台
- **启动日志统一** — 所有输出统一 `✅ 名称  详情` 格式
- **启动动画** — ZF3D AGENT方块字banner，CMD窗口标题显示版本号
- **蓝色提亮** — `--blue` 从 `#007ACC` 提亮到 `#2A9DFF`
- **日志面板** — 顶部拖拽条调整高度，复制/折叠按钮
- **面板边界保护** — 工作流面板拖出屏幕时自动拉回
- **记忆模块静默** — 后台归纳延迟30秒，失败不打印

---

## v3.1.1 (2026-07-09)

### 新增
- **云端出图节点(cloud_image)** — 免ComfyUI直接调用API出图，支持5个服务商：
  - 🆓 AgnesAI(全免费) — 邮箱注册即用
  - 🌟 Seedream(字节跳动) — 火山方舟API，送¥20
  - 🍌 Nano Banana(Google) — Gemini API，$10/天免费
  - 🎯 Grok(xAI) — grok-2-image
  - 🧠 GPT Image(OpenAI) — gpt-image-1
- **设置面板新增☁️云端出图页** — 4个API密钥输入框+注册链接+费用对比表+使用教程
- **模型配置页新增购买指南** — 12个LLM模型每个都有注册链接/充值方式/价格/免费额度
- **右键菜单重组** — 6个一级分类(基础/本地出图/云端出图/图片修改/视频/我的节点)
- **cloud_image配置面板** — 服务商下拉选择+宽高输入

### 修复
- **关闭静默更新** — 更新.启用改为false，防止开发版被远程release覆盖
- **版本号升至3.1.1** — 大于远程3.0.2，更新检查器不再触发更新

### ⚠️ 注意
- **发布新版本到GitHub/Gitee前**，需要把 `系统配置.json` 中 `更新.启用` 改回 `true`
- 另一个AI(comfyui优化)的改动也在同一次提交中，不会冲突

---

## v3.0.2 (2026-07-08)

### 新增
- **ComfyUI异步出图** — 提交后立即返回，后台轮询完成，全局锁防死锁
- **ComfyUI端口自动探测** — 配置设为auto，自动扫描8000-8010端口
- **节点商店右键菜单** — ComfyUI风格级联菜单，搜索+二级分类
- **ComfyUI工作流自动扫描** — 扫描_api.json文件，按文件夹分类显示
- **文本输入节点** — 零token，输入提示词直接输出
- **文本拼接节点** — 零token，拼接上游输入+固定文本
- **图片修改节点** — 零token，上传图片+注入提示词修改
- **视频生成节点** — 零token，文生视频/图生视频
- **单节点执行** — hover节点▶按钮，只执行该节点
- **节点禁用(D键)** — 禁用节点跳过执行，连线变灰虚线
- **分组禁用** — 选中分组按D，分组内所有节点一起禁用
- **图片大图查看器** — 滚轮缩放+中键平移+左右翻页+右键下一张
- **图片节点展开/收缩** — 多图展开到右侧面板，收缩翻页浏览
- **节点另存为弹窗** — 显示已有文件列表，可点击覆盖
- **员工对话SQLite持久化** — 自动存入员工对话记录表
- **员工配置自动备份** — 三层防护(临时文件+防误清空+备份恢复)
- **ComfyUI内置工作流** — z_image+qwen2512打包到公共区/工作流/
- **股票大模型插件** — 本地有则加载，外部用户自动跳过
- **文件树新建文件夹** — hover文件夹➕按钮
- **节点详情复制按钮** — 输入输出各一个📋
- **日志面板复制/折叠** — 执行日志右下角工具栏
- **启动动画** — "朱峰社区智能体 接管你成就你 zf3d.com出品"
- **启动加速** — 从5.7秒减到2.2秒

### 修复
- 工具员工参数名`工具→工具列表`导致生成图片失败
- 节点详情弹窗显示HTML源码（marked.parse二次解析）
- 节点详情文字无法框选复制（缺少user-select:text）
- ComfyUI宽高未注入工作流（新增_注入宽高函数）
- ComfyUI参数中英文双兼容（一键生图所有参数）
- POST请求缺User-Agent被ComfyUI拒绝
- 一键生图自动找工作流分支不等待不下载图片
- 反推操作保存目录传空字符串
- ComfyUI同时提交多个prompt导致DynamicVRAM死锁（全局锁）
- 员工改名不同步分类和节点画布
- 员工配置被误清空（防误清空+备份恢复）
- 节点拖拽时宽高变化（去掉mouseup保存offsetWidth）
- 节点内容撑宽节点（固定width+overflow:hidden+word-break）
- 分组resize手柄鼠标移动不匹配（除以wfZoom）
- 禁用节点标记未传到后端（nodesData加disabled字段）
- ComfyUI探测缓存端口变更后不更新（加存活验证+60秒过期）

### 清理
- 删除旧linux包、重复测试目录、维基百科.md、团队组织结构.md

---

## v3.0.1 (2026-07-07)

### 修复
- **工具员工执行报错** — 网页服务.py中`发送消息()`传了`工具=`而非`工具列表=`，导致工具员工（如备份员）调用LLM时报`unexpected keyword argument '工具'`
- **节点详情弹窗显示HTML源码** — showWfPopup对已是HTML的content二次执行`marked.parse()`，导致`<div>`标签被转义为纯文本显示
- **节点详情文字无法框选复制** — wf-popup-body缺少`user-select: text`，被父级`user-select: none`覆盖
- **节点工作流面板resize手柄不可拖拽** — 面板`overflow: hidden`裁剪了手柄，且手柄z-index遮挡画布导致员工/文件拖入失败。改为8方向手柄(左/右/上/下/四角)，面板去掉overflow:hidden，header/footer单独加圆角

### 优化
- **节点工作流面板8方向resize** — 新增顶部/左上/右上3个resize手柄，现在支持全部8方向自由调整面板大小
- **静态资源版本号更新** — 员工浮窗CSS/JS版本号更新为20260707b1，确保浏览器加载最新代码

### 清理
- 删除15个过时/冗余文件：升级路线工作计划.md、待开发功能设计方案.md、Houdini-Agent调研报告.md、test_bubble.py、test_fibonacci.py、test_tts.py、test.txt、hello.py、TTS测试.py、测试工作流.py、大理石房子.blend、2个旧linux包、CLAUDE.md、deploy.bat

---

## v3.1.0 (2026-07-05)

### 新增
- **3ds Max MCP整合** — 用户说"连接3dmax"即可连接3ds Max，自动安装环境（uv+依赖+插件），117个MCP工具直接控制3ds Max（创建物体/材质/修改器/动画/渲染/tyFlow等）。首次连接自动部署，后续秒连
- **绕路师** — AI失败教训自动记录系统。任务失败时分析失败模式→LLM提炼绕路方案→存入SQLite。下次同类任务自动注入"避坑指南"到提示词，AI不再踩同样的坑。高频失败（≥3次）自动写入Bug库
- **查询绕路记录操作** — AI可随时查询过往失败教训，快速定位问题
- **3ds Max/Houdini/Blender三路互斥** — 三个3D软件互斥锁定，连接3ds Max后Blender工具自动禁用，避免打架
- **120+中英参数名自动映射** — AI传中文参数名（操作/名称/代码）自动转英文（action/name/code），覆盖3ds Max/Houdini/Blender所有常见参数
- **启动动画** — ZF3D方块字banner+逐行动画+摘要列表统一渲染

### 修复
- 流式提前执行路径无try/except导致智能体崩溃 — 异常穿透到推理引擎
- install.py交互输入卡死 — 改为直接Python shutil复制gup/脚本/配置
- uv安装后PATH不生效 — 用完整路径替换配置中的"uv"命令
- MCP服务重复安装 — 安装标记检查，已安装直接跳过

### 优化
- 3ds Max操作全部走MCP工具，禁止用运行命令操控（提示词规则）
- 连接失败时直接提示用户，禁止AI自己排查环境（提示词规则）
- 40+个3ds Max同义词别名（连接/使用/启动/打开/安装 × 各种变体）
- 查Bug诊断报告包含绕路师记录段

## v3.0.0 (2026-07-04)

### 新增
- **Houdini 深度对接** — Bridge 架构连接 Houdini，AI 可直接创建/删除/连接节点、设置参数、写 VEX 代码、执行 Python、查询网络结构和几何信息、搜索节点类型、撤销重做、管理 NetworkBox 分组，共 10 个操作
- **Houdini 自动安装桥接** — AI 可自动检测电脑上的 Houdini 版本并安装 Bridge，支持热重载（无需重启 Houdini）
- **面板状态记忆** — 关闭/打开的文件面板、编辑器面板、缩略图面板状态自动保存，下次打开恢复
- **TTS 音量调节** — 右键 🔊 弹出音量滑块（0-100%），拖动实时保存，后端三层播放全部应用
- **录屏音量持久化** — 麦克风/系统音量滑块拖动时实时保存到 localStorage

### 修复
- `hou.undos.beginGroup/endGroup` 在 Houdini 20.0 不存在 — 新增 `_undo_group` 兼容层
- `execute_python` 不捕获 print 输出 — 用 `redirect_stdout` 捕获返回给 AI
- `execute_python` traceback 显示无关代码行 — 全局命名空间加 `__file__`，过滤内部帧
- `connect_nodes` 参数名语义反了导致连接方向错误 — 改为 `源节点路径/目标节点路径`
- `parameters` 传成 JSON 字符串而非 dict — `_auto_parse_args` 自动解析
- `hou.nodeTypeAll()` / `geo.vertices()` / `isBypassed()` 在 Houdini 20.0 不存在 — 全部加兼容回退
- `create_node` 失败只报 Invalid type — 新增可用类型列表帮助 AI 自我修正
- TTS 报错 `cannot access local variable 'threading'` — 删除局部 `import threading`
- 关闭面板后右侧空白 — 剩余面板自动扩展
- 浏览器缩窄到 500px 面板不收缩 — 降低 `min-width`，`flex-shrink:1`
- `showGallery()` 强制打开编辑器面板覆盖记忆 — 检查保存的状态
- 快速浮窗问答/翻译弹窗上方文本无法 Ctrl+C — 显式绑定复制和全选
- Houdini 插件 f-string 中文引号导致 SyntaxError — 插件加载失败

### 优化
- 删除不可用的 `Houdini视口截图` 操作，避免 AI 反复尝试
- 浏览器最小宽度从 ~788px 降到 ~428px
- `create_nodes_batch` 兼容中文键名（类型/名称/参数）

### 新增
- **快速浮窗翻译弹窗** — 选中文字Ctrl+~呼出轮盘选翻译，弹窗显示原文/译文/介绍/造句，流式输出+TTS朗读+拖拽
- **问答连通主对话** — 快速浮窗问答自动注入最近10轮主对话历史，问答结果回写主对话SQLite持久化，网页端可见
- **问答面板重构** — 上回复区(只读+滚动条)+下输入框，多轮对话追加显示，用户消息亮色靠右/机器人白色靠左+背景气泡
- **自动语音播报** — 右上角小喇叭(🔊/🔇)控制问答后自动TTS播报，默认开启，独立于轮盘朗读状态
- **轮盘设置面板** — 设置新增🎛️轮盘标签页：快捷键/轮盘半径/中心圆半径/字体大小/透明度/动画/7项颜色(color picker)/扇区管理(增删排序)/恢复默认，保存后热更新无需重启

### 修复
- 翻译弹窗不显示（`self._透明度`属性名不一致+`import json`缺失+`user32`未定义）
- 问答发送后面板消失（轮盘`<Leave>`事件未解绑触发延迟关闭）
- 问答TTS播报触发轮盘重绘（新增`_弹窗模式`标记区分轮盘/问答）
- 轮盘转问答时背景闪烁（先alpha=0再改尺寸）
- 截图选区遮罩覆盖截图（截图前先隐藏遮罩窗口）
- 轮盘API GET 404（GET/POST路由分开处理）

### 优化
- 颜色7项从配置JSON读取，不再硬编码
- 各模型添加"支持vision"字段
- 流式回复使用bot标签统一字体
- 问答面板字体统一10pt

## v2.8.0 (2026-07-02)

### 修复（4个核心Bug）
- **紧凑工具定义参数为空**（根因）— `获取紧凑工具定义()` 返回空 `properties: {}`，LLM无法知道每个操作的参数名，导致反复猜错参数名（如把"关键词"猜成"模式"），5次连续失败触发导师介入。修复为包含完整参数名+类型+说明+必填标记
- **_执行单个无异常兜底** — 工具执行函数无 `try/except`，操作注册中心抛出未预期异常时整个ReAct循环崩溃。添加 `except Exception` 兜底，返回结构化错误含堆栈信息
- **搜索代码参数别名不一致** — 系统将"关键词"映射为 `keyword` 暴露给LLM，但 `执行()` 方法不认 `keyword`（只认 `关键词`/`pattern`/`搜索词`/`关键字`）。`maxResults`→`max_results` 也有casing不匹配。3处搜索操作全部修复
- **Diff查看器不弹出** — `showDiffPage()` 触发逻辑被包裹在 `!hasFileChange` 条件中，但SSE流式阶段已把 `hasFileChange` 设为 `true`，导致条件永远为 `false`。去掉该条件

### 优化
- 紧凑工具定义与完整工具定义参数一致，LLM在每一步都能看到正确的参数名和类型

## v2.7.0 (2026-07-02)

### 新增
- **Bug追踪器** — 代码级Bug的SQLite持久化追踪，支持记录/解决/查询/搜索/统计，4个AI可调用操作（记录Bug/解决Bug/查询Bug列表/搜索Bug）
- **存储引擎读写分离** — 写连接+读连接，WAL下读不阻塞写
- **帮助系统第十九章** — AI测试智能体指南：通信协议、测试脚本模板、框选API格式、Bug录入流程、性能基线

### 修复（16个Bug）
- 系统托盘.py语法错误（user32 GetMessageW缺少点号）
- 浏览器引擎accessibility API废弃（改用CDP session）
- 上下文裁剪未调用（推理引擎构建消息列表后未裁剪，导致上下文超长）
- SSE流不关闭（完成事件后连接不关闭，导致客户端timeout）
- 流式回复逐词推送（50字符/0.5秒缓冲批量推送）
- 思考内容推送元数据而非实际LLM思考文本
- 简单任务4阶段LLM调用（反思阈值提高+经验保存改异步）
- 推理步数限制99→30
- 硬编码ffmpeg路径（改为shutil.which动态探测）
- ComfyUI诊断dir()不良写法
- 前端轮询优化（无下载时3s→10s慢速模式）
- 临时调试文件清理

### 性能提升
- 平均响应时间 80-120s → 2-8s（10-15倍）
- SSE事件数 11个/短句 → 1-2个/短句
- 25轮测试0崩溃0超时0空回复

## v2.5.0 (2026-06-30)

### 新增
- **独立下载面板** — 左侧固定面板，每个下载文件独立显示进度条/百分比/速度/ETA，可折叠，不遮挡对话区，页面加载即轮询
- **SHA256自动校验** — 下载完成后自动从URL提取哈希，分块计算文件SHA256对比，校验失败自动删除损坏文件，进度条显示"校验中"状态
- **损坏文件检测** — 文件存在但无控制文件(.aria2/.dlmeta/.parts)时自动删除重下，防止其他工具残留的损坏数据被误用
- **参数别名兼容** — 多线程下载(网址/url/URL→下载地址)、搜索代码(pattern→关键词)、后台执行(操作→操作名)

### 修复
- **aria2c进度不显示** — `--console-log-level=warn`改为`notice`，进度摘要不再被吞
- **后台下载线程崩溃** — SSE流关闭后`_SSE写入`抛`ValueError`导致下载中断，改为全异常捕获+`_安全推送进度`包装
- **aria2c参数重复** — `--max-connection-per-server`写了两次，后者覆盖前者；`--max-tries=0`无限重试改为有限重试
- **推理面板不隐藏** — 为保留下载进度条注释了`hideReasoningPanel()`，导致回答完成后推理面板始终可见
- **断点续传数据损坏** — Python多线程路径加载元数据时不校验part文件实际大小，元数据过期导致数据损坏
- **三套进度条打架** — 浮空条/推理面板条/独立面板同时轮询各自隐藏，统一为仅独立面板一个入口

### 优化
- 下载面板从右下角移到左侧(top:40px;left:0)，不遮挡聊天消息
- 轮询间隔2s→1s，进度条更新更及时
- 下载完成10秒后后端自动清理进度记录
- 推理面板对话结束立即隐藏，不再检查下载状态
- 下载面板原地更新DOM不重建，防止闪烁

## v2.4.0 (2026-06-30)

### 新增
- **音乐播放系统** — 搜索在线音乐→下载→ffmpeg转MP3→底部播放栏播放，支持播放列表/上下首/进度条拖拽/音量控制hover弹出竖向滑块
- **音乐库管理** — 同步音乐库JSON（去重、按歌手统计）、搜索音乐、已下载歌曲不重复下载
- **视频播放系统** — 搜索在线视频→下载视频流+音频流→ffmpeg合并MP4→中间区域本地播放
- **多线程下载重构** — 后台下载不阻塞对话、多文件并行进度条、aria2c加速支持、取消支持、ETA剩余时间
- **aria2c集成** — 自动检测aria2c，有则用（多连接加速），无则回退纯Python
- **朗读工具修复** — 换行符导致PowerShell命令断裂，改为临时文件传递文本
- **音频Range请求** — /api/audio支持206 Partial Content，可断点seek拖拽进度条
- **取消标志重置** — 修复停止后新建对话无法发送消息的问题

### 优化
- 音乐播放栏浮空在缩略图区域底部，不占空间不遮挡对话
- 音量控制改为hover弹出竖向滑块+点击循环档位
- 下载操作支持断点续传元数据
- .gitignore排除音乐/视频目录（个人隐私保护）

## v2.3.0 (2026-06-30)

### 新增
- **图片加工工具专业版**：独立页面编辑器，支持图层系统(混合模式+拖拽排序)、11种工具(笔刷/橡皮/图章/渐变/文字/加深减淡/模糊/填充/抓手/吸管/移动)、选区系统(矩形/椭圆/套索/魔棒+Ctrl加选/Alt减选布尔运算)、PS风格动态选项栏(每个工具有独立参数面板)、前景背景色(X切换/D恢复默认)、快捷键(B笔刷/E橡皮/[/]笔刷大小/Alt+Del/Ctrl+Del填充)、滚轮缩放+中键平移、撤销重做多图层
- **经验师模块**：任务成功后自动提炼可复用经验文档(Markdown+frontmatter)，任务开始前召回匹配经验注入提示词，标签索引+冗余合并(标签/关键词优先匹配，仅在合并时调用LLM)
- **OpenCV图片处理操作(8个)**：去水印(膨胀遮罩+羽化混合)、去杂物、调整亮度对比度、裁剪、缩放、模糊、灰度化、旋转
- **图片加工去水印算法改进**：膨胀遮罩3px+羽化混合消除接缝色差
- **亮度对比度GPU加速**：改用CSS filter不破坏画布变换
- **系统提示词禁止读取系统源代码文件规则**：防止AI读ComfyUI操作.py等触发授权循环

### 修复
- **XSS转义**：对话输出HTML转义防止注入
- **路径穿越防护**：文件操作路径校验防止目录穿越
- **Word格式保留**：替换文本时保留原格式(字体/颜色/粗体)
- **Excel类型保留**：替换文本时保留单元格数据类型
- **TTS串台**：多对话语音播报互不干扰
- **日志掩码**：API Key在日志中完整掩码
- **Git回滚前检查**：回滚前检查未提交工作+审计记录
- **调度器异常不再静默崩溃**：逐任务try-catch+诊断记录
- **ReAct上下文按需压缩**：防止长任务撞finish_reason=length
- **WinError10038套接字异常**：OSError加入静默+返回JSON加try-catch
- **multipart请求不再被UTF-8解码导致崩溃**：图片加工去水印上传修复
- **复制到同目录samefile检查**：resolve可能抛异常导致500
- **图片加工工具JS语法错误修复**：引号缺失+fitCanvas延迟

### 优化
- **图片加工工具5次迭代升级**：从基础涂抹工具发展为专业级图片编辑器
- **选区统一布尔运算**：矩形/椭圆/套索/魔棒统一Ctrl加选/Alt减选
- **框选改拖拽**：已选中项拖拽=移动，Shift+拖拽=复制，未选中项拖拽=框选
- **去水印算法改进**：膨胀遮罩+羽化混合消除接缝色差

## v2.2.1 (2026-06-29)

### 新增
- **文件管理器移动/复制**：后端新增 `移动()` 和 `复制()` 方法 + `/api/file-move` `/api/file-copy` 端点，支持文件/文件夹移动和复制，含权限校验+冲突检测+审计日志
- **缩略图悬浮操作按钮**：画廊缩略图hover时右上角出现🗑️删除/📦移动到/📋复制到三个圆形按钮，点击即可操作
- **HTML5拖拽移动/复制文件**：拖拽文件到左侧文件夹树或中间画廊文件夹=移动；Shift+拖拽=复制；支持单个或框选多个文件批量操作
- **画廊分页渲染**：文件夹内容过多时每页只渲染200项，底部出现「点击加载更多」按钮，彻底解决卡死问题
- **去背景工具**：独立页面（去背景工具.html），上传图片自动去背景生成透明PNG，可从图片查看器一键打开
- **我的电脑分组显示**：快捷访问（桌面/文档/下载/图片）和磁盘（C/D/E...带空间信息）分组展示，左侧树和中间画廊同步

### 修复
- 我的电脑点击后空白：后端有重复的旧版`/api/drives`路由（PowerShell版返回类型字段不匹配），已删除
- 左侧文件树磁盘大小被CSS flex挤掉不可见：改用`.ti-space`(flex-shrink:0)固定空间
- 框选与拖拽冲突：鼠标落在gallery-item上时不启动框选，让HTML5原生拖拽接管

## v2.2.0 (2026-06-27)

### 新增
- **Tavily智能搜索引擎集成**：网络搜索、网页抓取、网页分析三个操作全面接入Tavily API，返回AI优化的结构化结果（标题+URL+正文摘要+相关度评分+AI总结答案），无密钥时自动回退Bing/urllib
- **工具密钥管理界面**：设置→模型→工具密钥卡片，可在界面上查看Tavily密钥状态（掩码显示）、输入保存（加密存储），无需手动编辑JSON
- **后端 /api/tool-keys 端点**：GET返回工具密钥状态，POST保存加密
- **Linux安装支持**：install.sh一键安装脚本（检查Python→clone仓库→创建隐私区目录→初始化密钥→桌面快捷方式）、打包linux.py生成tar.gz便携包
- 流式渲染彩色callout样式（成功/错误/步骤/提示/测试五色，与最终渲染一致）
- 股票预测操作（LSTM神经网络，全JSON配置驱动）

### 优化
- callout样式柔化（降低饱和度+透明度，视觉更舒适）
- 密钥模板同步实际加密格式（多模型嵌套dict结构）

## v2.1.1 (2026-06-27)

### 修复
- 推理引擎 TypeError: 'NoneType' object is not callable — 任务账本类名被同名局部变量遮蔽，第一步操作成功后触发自动规划时崩溃
- 异常时未打印完整堆栈到终端，排查困难

### 新增
- 任务规划器（Plan-and-Execute + 动态重规划）
- 任务账本操作（查看/添加/完成任务）
- 存储引擎（SQLite对话存储 + 操作结果分层 + 任务经验）
- 诊断操作（查询/解决/清除运行错误 + 监控规则）
- 记忆操作（保存/搜索/遗忘记忆）
- 询问用户操作（多问题交互）
- 股票缓存模块
- 上下文管理器（Observation Masking + Token预算）
- 提示词构建器（三级缓存 + 记忆单轮缓存 + 用户习惯提取）

## v2.1.0 (2026-06-26)

### 新增
- 静默自动更新：启动3秒后后台检查，多源回退（Gitee优先→GitHub→自建服务器）
- EXE打包：PyInstaller一键打包，用户无需安装Python
- 更新检查器：GitHub Release + Gitee发行版自动检测，下载覆盖公共区+自动备份+失败回滚
- Gitee镜像仓库：国内用户更新走Gitee，不依赖翻墙

### 修复
- 运行命令超时卡死：subprocess.run超时后子进程成孤儿，改用Popen+taskkill /T杀进程树
- LLM工具调用参数截断：max_tokens不足导致JSON不完整，json.loads失败后静默返回{}→AI收到"缺少必填参数"误报。现在检测finish_reason=length并提示AI拆分文件用追加文件分批写入
- max_tokens 4096→8192，从源头减少截断
- 跨对话记忆混淆：记忆注入标签"相关记忆""近期摘要"改为明确标注"非当前对话历史，不要混入当前对话"
- Ctrl+A被拦截：焦点在对话输入框/编辑器时onSelectionKeyDown仍拦截，增加TEXTAREA/INPUT检查+mediaView可见性检查

### 优化
- 更新源优先级：Gitee优先（国内用户），GitHub次之，自建服务器兜底

## v2.0.0 (2026-06-25)

### 新增
- 全JSON配置驱动的AI智能体框架
- 模块化架构：对话/记忆/任务/浏览器四大模块
- 事件制记忆系统：自动摘要、冗余清理、长上下文适应
- 双引擎自我进化：主引擎稳定发布 + 工作引擎大模型开发
- 文件权限铁律：未授权路径弹窗询问，3次后自动升级永久授权
- 40个内置操作：文件/代码/Git/系统/网络/高级/构建
- 内置Web界面：对话/文件管理/编辑器/日志/配置
- 全透明机制：LLM原始请求/响应完全可见
- 隐私分区：公共区可发布，隐私区绝不外泄
- 一键发布打包：自动隐私泄露扫描
- 支持DeepSeek/豆包/智谱/Kimi/Claude/Gemini等模型
- pytest自动化测试套件（92个用例）

### 技术栈
- Python 3.8+（纯标准库，零外部依赖）
- 前端：纯HTML/CSS/JavaScript（无框架）
- 存储：全JSON文件（无数据库）
